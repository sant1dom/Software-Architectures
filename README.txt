Setup:

1) Clone the project from GitHub
2) Create a new Python Project in the Project Dir
3) Add the module "requests"
4) Open the shell and go to the Project Dir
5) type "docker compose build"
6) type "docker compose up"
7) type "python energy_production/main.py"
